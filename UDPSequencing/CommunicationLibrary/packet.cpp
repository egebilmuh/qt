#include "packet.h"
#include <cstring>

//bool operator <(const Packet &lhs, const Packet &rhs)
//{

//}

//bool operator >(const Packet &lhs, const Packet &rhs)
//{
//}


//Packet::Packet()
//    :id (0)
//    ,sequence (0)
//{
//    std::fill(data, data+dataSize,0);
//}

//Packet::Packet(const Packet &p)
//{
//    this->id = p.id;
//    this->sequence = p.sequence;
//    std::memcpy(data, p.data, dataSize*sizeof(data));
//}

//Packet &Packet::operator=(const Packet p)
//{
//    this->id = p.id;
//    this->sequence = p.sequence;
//    std::memcpy(data, p.data, dataSize*sizeof(data));

//    return *this;
//}
